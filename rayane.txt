so the feautre enginering is used to create new features from existing ones, 
or remove features that are irrelevant to the study, 
in our case, we created many features that are relevant , to feed to the model
to imporve it's accracy , so that the model can distinguish from different types of urls
